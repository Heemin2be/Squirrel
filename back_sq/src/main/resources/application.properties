# ===================================================================
# DataSource (Database) Settings
# ===================================================================
# Database Connection URL
spring.datasource.url=jdbc:mysql://localhost:3306/squirrel_project_db?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8

# Database username and password
spring.datasource.username=root
spring.datasource.password=Kdkfldk02@

# Specify the driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================================================
# JPA / Hibernate Settings
# ===================================================================
# Automatically create/update database tables based on @Entity classes
spring.jpa.hibernate.ddl-auto=update

# Show executed SQL queries in the console (useful for debugging)
spring.jpa.show-sql=true

# Format the shown SQL queries
spring.jpa.properties.hibernate.format_sql=true

app.jwt.secret=daramgol-jwt-secret-key-should-be-long-32chars!!!!
app.jwt.expiration-ms=3600000
